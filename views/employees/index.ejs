<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Employee List</title>
    <link rel="stylesheet" href="../index.css" />
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
      integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z"
      crossorigin="anonymous"
    />
    <style>
          /* Custom CSS for table header */
          th {
            background-color: black;
            color: white;
            padding: 10px;
        }
       

        .nav-item{
            color: black;

        }
        a{
            text-decoration: none;
            color: black;
            margin-right: 20px;
            font-weight: 400;
            font-family: Poppins;
            
        }
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 20px;
            padding: 20px;
        }
       
    </style>

      
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/">Mehak Ramzan</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
    
        <div class="collapse navbar-collapse" id="navbarSupportedContent" >
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a  href="/employees">Home</a>
                </li>
                <li class="nav-item">
                    <a  href="/employees/add">Add New Employee</a>
                </li>
                
                <li class="nav-item">
                    <a  href="/auth/logout">Logout</a>
                </li>
                <!-- Add more menu items as needed -->
            </ul>
        </div>
    </nav>
   
    <div class="container mt-5">
        <h1 class="mb-4"  >Employee List</h1>

        <!-- Table to display employees -->
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Number</th>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Position</th>
                    <th>Department</th>
                    <th>Action</th> <!-- Edit and Delete buttons -->
                </tr>
            </thead>
            <tbody>
                <% employees.forEach((employee, index) => { %>
                    <tr>
                        <td><%= index + 1 %></td>
                        <td><%= employee.name %></td>
                        <td><%= employee.email %></td>
                        <td><%= employee.position %></td>
                        <td><%= employee.department %></td>
                        <td>
                            <a href="/employees/edit/<%= employee._id %>" class="btn btn-primary btn-sm">Edit</a>
                            <a href="/employees/delete/<%= employee._id %>" class="btn btn-danger btn-sm">Delete</a>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.bundle.min.js" integrity="sha384-ho+j7jyWK8fNQe+A12Hb8AhHbQFhbbZvbPS/QZ5nOdex+hj9QCI3zW0tYF1v7BbE" crossorigin="anonymous"></script>
  </body>
</html>
